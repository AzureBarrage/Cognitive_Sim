version: '3.8'

services:
  cognitive-agent:
    build: .
    container_name: cognitive_sim
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data  # Persist memory to host
    restart: unless-stopped
    environment:
      - ENV=production
